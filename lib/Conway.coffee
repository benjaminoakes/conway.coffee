class DeadCell
  tick: (neighborCount) ->
    # if neighborCount == 3
    #   new AliveCell
    # else
    #   new DeadCell

class AliveCell
  # tick: (neighborCount) ->
  #   if neighborCount < 1 || neighborCount > 3
  #     new DeadCell
  #   else
  #     new AliveCell

class Ticker

class Grid
