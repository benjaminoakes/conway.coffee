class AliveCell
  # tick: (neighborCount) ->
  #   if neighborCount < 1 || neighborCount > 3
  #     new DeadCell
  #   else
  #     new AliveCell

class DeadCell
  # tick: (neighborCount) ->
  #   if neighborCount == 3
  #     new AliveCell
  #   else
  #     new DeadCell

class Ticker

class Grid
